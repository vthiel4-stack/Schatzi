<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Für Selina ❤️</title>
  <style>
    body {
      margin:0;
      display:flex;
      align-items:center;
      justify-content:center;
      height:100vh;
      background:linear-gradient(135deg,#0f2027,#203a43,#2c5364);
      font-family:sans-serif;
      color:white;
      text-align:center;
      overflow:hidden;
    }
    .heart{
      width: 140px; height: 140px;
      position: relative;
      transform: rotate(-45deg);
      background: linear-gradient(45deg, #ff4d6d, #ff758f);
      border-radius: 16px;
      box-shadow: 0 18px 45px rgba(255,105,135,.5);
      animation: beat 1.2s ease-in-out infinite;
      margin: 0 auto 20px;
    }
    .heart::before,
    .heart::after{
      content:"";
      position:absolute;
      width:140px; height:140px;
      background: inherit;
      border-radius:50%;
    }
    .heart::before{ top:-70px; left:0; }
    .heart::after{ left:70px; top:0; }
    @keyframes beat{
      0%,100%{ transform: rotate(-45deg) scale(1); }
      25%{ transform: rotate(-45deg) scale(1.06); }
      50%{ transform: rotate(-45deg) scale(0.98); }
      75%{ transform: rotate(-45deg) scale(1.06); }
    }
    h1{font-size:2.2em; margin:0; text-shadow:0 0 15px rgba(255,105,135,.4);}
    p{margin:8px 0 0; opacity:0.8;}
    .floater{
      position: fixed;
      bottom: -40px;
      left: 0;
      user-select:none;
      pointer-events:none;
      animation: rise linear forwards;
      filter: drop-shadow(0 6px 10px rgba(255,105,135,.35));
    }
    @keyframes rise{
      0%{ transform: translateY(0) scale(1) rotate(0deg); opacity:0; }
      10%{ opacity:1; }
      100%{ transform: translateY(-110vh) scale(1.4) rotate(360deg); opacity:0; }
    }
  </style>
</head>
<body>
  <div>
    <div class="heart"></div>
    <h1>Ich liebe dich, Selina! 💖</h1>
    <p>Klick auf den Bildschirm und es regnen mehr Herzen 💕</p>
  </div>

  <script>
    function makeHeart(){
      const s = document.createElement('span');
      s.className = 'floater';
      s.textContent = Math.random() > 0.3 ? '❤' : '💕';
      s.style.left = Math.random()*100 + 'vw';
      s.style.animationDuration = (6 + Math.random()*5).toFixed(2) + 's';
      s.style.fontSize = (16 + Math.random()*40).toFixed(0) + 'px';
      document.body.appendChild(s);
      s.addEventListener('animationend', () => s.remove());
    }
    for(let i=0;i<20;i++){
      setTimeout(makeHeart, i*200);
    }
    ['click','touchstart'].forEach(evt => {
      document.addEventListener(evt, () => {
        for(let i=0;i<10;i++){ setTimeout(makeHeart, i*100); }
      });
    });
  </script>
</body>
</html>
